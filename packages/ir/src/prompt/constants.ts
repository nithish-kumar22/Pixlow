/**
 * Prompt constraints for M07 — Prompt Validation & System Prompt Builder.
 * Use these in validation and in frontend (e.g. M16 textarea max length).
 */
export const PROMPT_MIN_LENGTH = 10;
export const PROMPT_MAX_LENGTH = 4000;
